/**
 * Public API of the landing feature. Everything else inside this folder is
 * internal — other features and the app router import only from here.
 */
export { LandingView } from "./presentation/views/landing-view";
export type { GreetingDto } from "./application/dto/greeting.dto";
